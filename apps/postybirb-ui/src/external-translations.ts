import { MessageDescriptor } from '@lingui/core';
import { msg } from '@lingui/macro';

export const externalTranslations: Record<string, MessageDescriptor> = {
  Description: msg`Description`,
  Tags: msg`Tags`,
  Title: msg`Title`,
  Rating: msg`Rating`,
};
