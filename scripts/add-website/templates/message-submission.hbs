import { DescriptionField{{#if supportsTags}}{{else}}, TagField {{/if}} } from '@postybirb/form-builder';
import { DescriptionType, DescriptionValue, DefaultDescriptionValue{{#if supportsTags}}{{else}}, TagValue, DefaultTagValue{{/if}} } from '@postybirb/types';
import { BaseWebsiteOptions } from '../../../models/base-website-options';

export class {{pascalWebsiteName}}MessageSubmission extends BaseWebsiteOptions {
  @DescriptionField({
    descriptionType: DescriptionType.HTML
  })
  description: DescriptionValue = DefaultDescriptionValue();
  {{#if supportsTags}}
  {{else}}

  @TagField({ hidden: true })
  tags: TagValue = DefaultTagValue();
  {{/if}}
}
