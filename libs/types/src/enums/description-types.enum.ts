export enum DescriptionType {
  BBCODE = 'bbcode',
  CUSTOM = 'custom',
  RUNTIME = 'runtime', // Used for determining at-runtime dynamic DescriptionType
  HTML = 'html',
  MARKDOWN = 'markdown',
  NONE = 'none',
  PLAINTEXT = 'plaintext',
}
